# MERN Tutorial

## Dependencies

- node
- yarn
- typescript
- mongodb (mongosh)
- docker
- docker-compose

## Database

Run using:

```bash
cd database && yarn start
```

Connect via terminal with:

```bash
mongosh admin -u <user> -p <password>
```
