{
  "name": "mern-database",
  "version": "1.0.0",
  "author": "Joe Worsfold",
  "license": "MIT",
  "scripts": {
    "start": "source ../backend/.env && docker-compose up -d",
    "stop": "docker-compose stop",
    "down": "docker-compose down",
    "connect": "source ../backend/.env && mongosh mern-app -u ${DB_USER} -p ${DB_PASSWORD}"
  }
}
